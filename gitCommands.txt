First Add the final
git add File Name

Then commit the file

git commit -m "any message" fileName

Then Push the file to GitHub

git push -u origin master

Command to check the status of file added or not
git status 


If we want to modify the file exist in our system then we just meed to commit and push that file to gitHub.
There is no need to add that file again
